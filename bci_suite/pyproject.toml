[project]
name = "bci_suite"
version = "0.1.0"
description = "Multimodal (facial + EEG) affective computing suite"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["bci_suite*"]
